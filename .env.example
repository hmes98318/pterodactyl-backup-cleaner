# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=pterodactyl
DB_PASSWORD=your_password
DB_NAME=panel

# Pterodactyl backup directory path
BACKUP_PATH=/mnt/pterodactyl/backup

# Cleanup Schedule Configuration
# Cron expression for cleanup schedule (default: 2 AM daily)
# Format: "minute hour day month weekday"
# Examples:
#   "0 2 * * *"    - Every day at 2:00 AM
#   "0 */6 * * *"  - Every 6 hours
#   "0 0 */3 * *"  - Every 3 days at midnight
GC_SCHEDULE=0 2 * * *
